<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Products</title>
</head>

<body>
    <!--Capitalize the first letter of each word in h1-->
    <h1 style="text-transform: capitalize">
        <%=category%> Products
    </h1>
    <ul>
        <%for(let product of products){%>
            <li>
                <!--because each instance in products we named product, so here is product.  _id is one of key names of each product-->
                <!--A clikable link of the product with specific id-->
                <a href="/products/<%=product._id%>">
                    <%= product.name %>
                </a>
            </li>
            <%}%>
    </ul>
    <a href="/products/new">New Product</a>
    <%if(category !='ALL' ){%>
        <a href="/products">All Product</a>
        <%}%>
</body>

</html>